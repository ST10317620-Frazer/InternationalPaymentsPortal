version: 2.1
jobs:
  build:
    docker:
      - image: node:18
    steps:
      - checkout
      - run: cd backend && npm install && npm test
      - run: cd frontend && npm install && npm test
workflows:
  build-and-test:
    jobs:
      - build
